{
  "scripts": {
    "start": "shopify theme dev --store=ubuntu-made && npx tailwindcss -i ./tailwind-input.css -o ./assets/tailwind-output.css --minify --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.1.5"
  },
  "dependencies": {
    "@tailwindcss/line-clamp": "^0.4.0"
  }
}
