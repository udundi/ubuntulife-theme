<div class="flex flex-col items-center">
  <div class="bg-neutral-50 w-full flex flex-col items-center px-8 py-14 mb-8">
    <h1 class="font-black text-3xl lg:text-4xl">
      {{ page.title | upcase }}
    </h1>
  </div>

  <div class="max-w-lg lg:max-w-4xl px-8 [&_p]:mb-4 tracking-wide font-medium">
    {{ page.content }}
  </div>
</div>
